{
  "name": "ma2f-aquasachet-functions",
  "version": "2.0.0",
  "description": "Cloud Functions Firebase pour MA2F AquaSachet — Validation serveur, custom claims, audit log, sauvegardes",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
